@{
    ViewData["Title"] = "Other Service Page";
}


<h2>Upload to Azure</h2>

<hr>

@model FormViewModel
<form asp-controller="Service" asp-action="Submit" method="POST">
    <div class="form-group">
        <label>Upload your file</label>
        <input asp-for="filePath" type="file" class="form-control"/>

        <button type="submit">Submit</button>
    </div>
</form>

<hr>

@{
    if(ViewData["error"] != null)
    {
        <h2 style="color:red">
            @ViewData["error"]
        </h2>
    }
}

<h2>Recent Requests</h2>
<p style="color:blue">

@{
    if(ViewData["results"] != null)
    {
        @ViewData["results"]
    } else {
        <p>Nothing to see here...</p>
    }
}   
</p>
<!-- <form method="post" enctype="multipart/form-data" asp-controller="Service" asp-action="Submit">
    <div class="form-group">
        <div class="col-md-10">
            <p>Upload a file using this form</p>
            <input type="file" name="img" />
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" value="Upload" />
        </div>
    </div>
</form> -->

